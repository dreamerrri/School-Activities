# Python program that will ask the user for number input.
# Then prints the Name inside the code based on the value of inputted number.

num = int(input("Enter a number: "))

i = 1
while i <= num:
    print("Your name")
    i = i + 1
